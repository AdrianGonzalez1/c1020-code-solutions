/* eslint-disable no-unused-vars */
function last(array) {
  for (var i = 0; i < array.length; i++) {
    return array[array.length - 1];
  }
  return undefined;
}

// create a for loop to iterate through the length of the array variable
// target the last element using length property - 1 and return the result
// return undefined if no value is within the array variable
