/* eslint-disable no-unused-vars */
function head(array) {
  if (array === null) {
    return undefined;
  }
  return array[0];
}

// don't need to loop through anything since getting the first index of array
// need to check if there is a value in array if not return undefined
